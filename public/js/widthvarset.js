


var screenWidth = ($(screen).width() * .9);
var screenHeight = ($(window).height() * .9);

('#smurf').height(screenHeight);
('#smurf').attr('width').val(screenWidth);

console.log(screenWidth, screenHeight);
